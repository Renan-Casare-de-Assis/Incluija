package br.com.fiap.incluija.ui.theme

import androidx.compose.ui.graphics.Color

// ============================================================
//  IncluiJá – Paleta de Cores
//  Gradiente principal: Ouro → Coral → Lilás
// ============================================================

// --- Ouro (Gold) ---
val Gold80        = Color(0xFFFFE599)   // claro – dark theme
val Gold40        = Color(0xFFFFD166)   // principal – light theme  ★ cor do logo
val GoldDark      = Color(0xFFE6B83E)   // hover / pressed
val GoldLight     = Color(0xFFFFF6DC)   // surface / background suave

// --- Coral (Red-Orange) ---
val Coral80       = Color(0xFFFFADAD)   // claro – dark theme
val Coral40       = Color(0xFFFF6B6B)   // principal – light theme  ★ cor do logo
val CoralDark     = Color(0xFFE04848)   // hover / pressed
val CoralLight    = Color(0xFFFFE5E5)   // surface / background suave

// --- Lilás (Violet) ---
val Violet80      = Color(0xFFDFB3FF)   // claro – dark theme
val Violet40      = Color(0xFFC77DFF)   // principal – light theme  ★ cor do logo
val VioletDark    = Color(0xFFA855E8)   // hover / pressed
val VioletLight   = Color(0xFFF3E5FF)   // surface / background suave

// --- Neutros Escuros (Dark backgrounds) ---
val Ink           = Color(0xFF0F0F14)   // fundo principal dark
val InkMid        = Color(0xFF1A1A26)   // fundo secundário dark
val InkCard       = Color(0xFF22222F)   // superfície de card dark
val InkBorder     = Color(0xFF2E2E40)   // bordas dark

// --- Neutros Claros (Light backgrounds) ---
val OffWhite      = Color(0xFFF8F7FF)   // fundo principal light (levemente lilás)
val Surface       = Color(0xFFFFFFFF)   // superfície branca pura
val GrayMid       = Color(0xFF8A8A9E)   // texto secundário
val GrayLight     = Color(0xFFE2E1EE)   // bordas / divisores light